# БОТ — СВЯЩЕННАЯ КОРОВА 🐄

## Правила работы с ботом:

### ❌ НЕЛЬЗЯ:
1. Деплоить без тестов
2. Править `bot.py`, `telegram_bot.py`, `pipeline.py` без крайней необходимости
3. Деплоить если тесты не прошли
4. Трогать зависимости без проверки

### ✅ МОЖНО безопасно:
1. Менять Django views (не влияет на бота)
2. Менять templates (фронтенд)
3. Добавлять новые API endpoints
4. Менять настройки Django

## 🛡️ Защита бота:

### 1. Автотесты перед деплоем
Каждый деплой автоматически:
- Проверяет что файлы на месте
- Проверяет что `pipeline.py` не поврежден
- Если тесты не прошли → деплой ОТМЕНЯЕТСЯ

### 2. Автоматический мониторинг (настрой один раз)

Запусти:
```bash
./setup_monitoring.sh
```

Это настроит:
- ✅ Проверку бота каждые 5 минут
- ✅ Уведомления в Telegram если бот упал
- ✅ Логи мониторинга

### 3. Ручная проверка здоровья

В любой момент:
```bash
./check_bot_health.sh
```

Покажет:
- Работает ли бот
- Работает ли Django
- Есть ли ошибки в логах

## 📋 Чеклист перед деплоем:

1. ✅ Запусти `./test_bot.sh` — тесты прошли?
2. ✅ Проверь что не трогал bot.py/telegram_bot.py/pipeline.py
3. ✅ Запусти `./deploy_from_local.sh`
4. ✅ После деплоя: `./check_bot_health.sh` — бот ОК?

## 🚨 Если бот упал:

### Быстрое восстановление:

```bash
# 1. Откатись к последнему рабочему коммиту
cd ~/Projects/shoesbot
git log --oneline  # Найди последний рабочий коммит
git reset --hard COMMIT_ID

# 2. Задеплой откат
./deploy_from_local.sh

# 3. Проверь
./check_bot_health.sh
```

### Или восстанови на VM:

```bash
ssh gcp-shoesbot
cd ~/shoesbot
git log --oneline
git reset --hard COMMIT_ID
sudo systemctl restart shoesbot.service
```

## 📊 Как узнать что бот работает:

1. **Автоматически:** Настрой мониторинг (./setup_monitoring.sh)
2. **Вручную:** ./check_bot_health.sh
3. **В Telegram:** Отправь /ping боту
4. **В логах:** `ssh gcp-shoesbot 'tail -f ~/shoesbot/bot.log'`

---

**ПОМНИ: Бот — это критичная часть системы!**

Если сомневаешься — запусти тесты. Лучше лишний раз проверить чем сломать прод.

