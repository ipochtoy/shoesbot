# ShoesBot - Полная инструкция по установке

Система автоматической обработки фото товаров с AI-анализом для маркетплейсов (Shopify, eBay и т.д.)

## Что это делает?

1. **Telegram бот принимает фото** товаров (обувь, одежда, часы, электроника)
2. **AI автоматически:**
   - Распознает баркоды и QR коды
   - Генерирует описание товара
   - Создает фото модели в одежде (FASHN AI)
   - Улучшает фото (меняет фон, добавляет тени)
3. **Веб-интерфейс** для управления карточками
4. **Интеграция с Shopify** и другими платформами

---

## Требования

- macOS или Linux
- Python 3.9+
- 2GB свободного места
- Интернет

---

## Шаг 1: Установка

### 1.1 Склонировать проект

```bash
cd ~/Projects
git clone https://github.com/ipochtoy/shoesbot.git
cd shoesbot
```

### 1.2 Создать виртуальное окружение

```bash
python3 -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

### 1.3 Установить зависимости

```bash
pip install -r requirements.txt
```

---

## Шаг 2: Получение API ключей

### 2.1 Telegram Bot (ОБЯЗАТЕЛЬНО)

**Что:** Бот для приема фото

**Как получить:**
1. Открой Telegram
2. Найди @BotFather
3. Отправь `/newbot`
4. Придумай имя: "Мой Товарный Бот"
5. Придумай username: `my_product_bot`
6. **Скопируй токен** (выглядит как `1234567890:ABCdefGHIjklMNOpqr...`)

**Куда сохранить:** В `.env` как `BOT_TOKEN=...`

**Стоимость:** Бесплатно

---

### 2.2 OpenAI API (ОБЯЗАТЕЛЬНО)

**Что:** AI для генерации описаний товаров

**Как получить:**
1. Открой https://platform.openai.com/api-keys
2. Зарегистрируйся (нужна карта)
3. Жми "Create new secret key"
4. **Скопируй ключ** (показывается один раз!)

**Куда:** В `.env` как `OPENAI_API_KEY=sk-proj-...`

**Стоимость:** 
- $5 бонус при регистрации
- ~$0.01 за описание товара (500 описаний на $5)

---

### 2.3 FASHN AI (ОПЦИОНАЛЬНО - для одежды)

**Что:** Генерация фото модели в одежде

**Как получить:**
1. Открой https://app.fashn.ai/api
2. Зарегистрируйся
3. "Get API Key"
4. **Скопируй ключ** (fa-...)

**Куда:** `.env` как `FASHN_API_KEY=fa-...`

**Стоимость:**
- 1 кредит = 1 фото
- ~$0.05 за фото
- Если продаешь **НЕ одежду** - можно НЕ покупать

---

### 2.4 Google Vision (ОПЦИОНАЛЬНО)

**Что:** Распознавание текста на бирках

**Как получить:**
1. Открой https://console.cloud.google.com/
2. Создай проект
3. Включи "Vision API"
4. Создай API ключ в "Credentials"

**Куда:** `.env` как `GOOGLE_VISION_API_KEY=AIza...`

**Стоимость:**
- 1000 запросов/месяц бесплатно
- Потом $1.50 за 1000 запросов

**Можно пропустить** - система работает и без этого

---

### 2.5 Google Custom Search (ОПЦИОНАЛЬНО)

**Что:** Поиск стоковых фото товара

**Как получить:**
1. https://developers.google.com/custom-search/v1/introduction
2. Получи API key
3. Создай Custom Search Engine
4. Скопируй Engine ID

**Куда:** 
```
GOOGLE_CUSTOM_SEARCH_API_KEY=...
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=...
```

**Можно пропустить** - не критично

---

## Шаг 3: Настройка .env файла

Создай файл `.env` в корне проекта:

```bash
# ОБЯЗАТЕЛЬНЫЕ
BOT_TOKEN=твой_telegram_bot_token
OPENAI_API_KEY=sk-proj-твой_ключ

# ОПЦИОНАЛЬНЫЕ (для одежды)
FASHN_API_KEY=fa-твой_ключ

# ОПЦИОНАЛЬНЫЕ (улучшают распознавание)
GOOGLE_VISION_API_KEY=AIza...
GOOGLE_CUSTOM_SEARCH_API_KEY=AIza...
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=...

# Для буферного бота (если нужен)
BUFFER_BOT_TOKEN=другой_бот_токен

# Для интеграции с твоим складом (опционально)
POCHTOY_API_URL=https://твой-склад.com/api/...
POCHTOY_API_TOKEN=твой_токен
```

---

## Шаг 4: Запуск

### 4.1 Создать базу данных

```bash
cd shoessite
python manage.py migrate
```

### 4.2 Создать админа для веб-интерфейса

```bash
python manage.py createsuperuser
# Введи username: admin
# Email: твой@email.com
# Password: (придумай пароль)
```

### 4.3 Запустить веб-сервер

```bash
python manage.py runserver 0.0.0.0:8000
```

Открой в браузере: http://127.0.0.1:8000/admin/
Логин с созданным username/password

### 4.4 Запустить Telegram бота

**В ОТДЕЛЬНОМ терминале:**

```bash
cd ~/Projects/shoesbot
source .venv/bin/activate
python shoesbot/telegram_bot.py
```

### 4.5 Запустить Cloudflared (для FASHN AI)

**Только если используешь FASHN AI для одежды.**

**В ТРЕТЬЕМ терминале:**

```bash
cloudflared tunnel --url http://localhost:8000
```

Скопируй URL из вывода (типа `https://xxx-yyy-zzz.trycloudflare.com`) и добавь в `.env`:

```bash
CLOUDFLARED_URL=https://твой-url.trycloudflare.com
```

---

## Использование

### Основной workflow

1. **Отправь фото в Telegram бота** (2-10 фото одного товара)
2. Бот автоматически:
   - Распознает баркоды
   - Создаст карточку
   - Сгенерирует описание
3. **Открой веб-интерфейс** http://127.0.0.1:8000/admin/photos/photobatch/
4. **Отредактируй карточку** (цена, размер, описание)
5. **Обработай фото:**
   - Кнопка 👤 - создать фото модели (для одежды)
   - Кнопка ✨ - улучшить фон
6. **Экспортируй в Shopify** (кнопка появится когда настроишь Shopify)

### Буферный режим (для массовой загрузки)

Если нужно загрузить 50+ фото разных товаров:

1. Создай второго бота в @BotFather
2. Добавь `BUFFER_BOT_TOKEN=...` в `.env`
3. Запусти: `python buffer_bot.py`
4. Отправь все фото в буферного бота
5. Открой http://127.0.0.1:8000/photos/sorting/
6. Drag & drop сортируй фото по группам
7. Жми "Отправить" на каждой группе

---

## Настройка для часов (вместо обуви/одежды)

### Отключить FASHN AI

FASHN AI работает только для одежды. Для часов НЕ нужен.

В `.env` **НЕ добавляй** `FASHN_API_KEY` - кнопки 👤 и ✨ не появятся.

### Настроить AI промпты под часы

Файл: `shoessite/photos/ai_helpers.py`

Найди функцию `generate_product_summary()` и измени промпт:

```python
# Было:
"Generate product description for clothing/shoes"

# Стало:
"Generate product description for watches and accessories"
```

### Добавить поля для часов

Файл: `shoessite/photos/models.py`

Можешь добавить специфичные поля:

```python
class PhotoBatch(models.Model):
    # ... существующие поля ...
    
    # Для часов
    watch_brand = models.CharField(max_length=200, blank=True)  # Casio, Rolex
    watch_model = models.CharField(max_length=200, blank=True)  # G-Shock GA-110
    watch_mechanism = models.CharField(max_length=50, blank=True)  # Quartz, Automatic
    watch_condition = models.CharField(max_length=50, blank=True)  # New, Used, Vintage
```

После изменений запусти:
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Частые проблемы

### Бот не отвечает

**Проверь:**
```bash
# Бот запущен?
ps aux | grep telegram_bot

# Токен правильный?
grep BOT_TOKEN .env
```

**Решение:** Перезапусти бота

### FASHN не работает

**Проверка:**
- Cloudflared запущен?
- URL в `.env` обновлен?
- Кредиты есть? https://app.fashn.ai/api

**Решение:** 
```bash
pkill cloudflared
cloudflared tunnel --url http://localhost:8000
# Обнови CLOUDFLARED_URL в .env
```

### Баркоды не распознаются

**Улучшение:**
1. Фотографируй баркод крупно и четко
2. Хорошее освещение
3. Баркод ровно (не под углом)
4. Или используй кнопку "Добавить баркод" вручную

### Веб-интерфейс не открывается

**Проверь:**
```bash
# Django запущен?
curl http://127.0.0.1:8000

# Если нет - запусти:
cd shoessite
python manage.py runserver 0.0.0.0:8000
```

---

## Обновление системы

Когда выйдут новые версии:

```bash
cd ~/Projects/shoesbot
git pull origin main
source .venv/bin/activate
pip install -r requirements.txt --upgrade
cd shoessite
python manage.py migrate
```

Перезапусти все сервисы.

---

## Бэкап данных

### Бэкап базы данных

```bash
cd shoessite
python manage.py dumpdata photos > backup_$(date +%Y%m%d).json
```

### Восстановление

```bash
python manage.py loaddata backup_20241105.json
```

### Бэкап фото

```bash
tar -czf photos_backup_$(date +%Y%m%d).tar.gz shoessite/media/
```

---

## Очистка тестовых данных

Перед передачей другу очисти:

```bash
cd shoessite
python manage.py shell

# В shell:
from photos.models import PhotoBatch, Photo, BarcodeResult, PhotoBuffer
PhotoBatch.objects.all().delete()  # Удалит все карточки
PhotoBuffer.objects.all().delete()  # Очистит буфер
```

Удали фото с диска:
```bash
rm -rf shoessite/media/photos/*
rm -rf shoessite/media/buffer/*
```

---

## Адаптация под часы

### Изменить название

1. В `shoessite/photos/templates/photos/product_card.html`:
   - Найди `<h1>SHOESBOT</h1>`
   - Замени на `<h1>WATCHBOT</h1>` или свое название

2. Измени заголовки:
   - "Карточки товаров" → "Каталог часов"
   - "Фото" → "Фото часов"

### Изменить категории

В `shoessite/photos/ai_helpers.py` - промпт для AI:

```python
# Было:
system_prompt = "You are analyzing product photos of shoes and clothing"

# Стало:
system_prompt = "You are analyzing product photos of watches and accessories"
```

### Добавить специфичные поля

Смотри раздел выше про watch_brand, watch_model и т.д.

---

## Поддержка

**Проблемы с установкой:**
- Проверь логи: `tail -f shoessite/db.sqlite3`
- Telegram бот: логи в `bot.log`

**Вопросы по коду:**
- Основной код: `shoesbot/telegram_bot.py`
- Веб-интерфейс: `shoessite/photos/views.py`
- AI обработка: `shoessite/photos/ai_helpers.py`

**GitHub Issues:**
https://github.com/ipochtoy/shoesbot/issues

---

## Стоимость использования

**Минимальная конфигурация (только распознавание):**
- Telegram бот: бесплатно
- OpenAI GPT-4o-mini: ~$5-10/месяц на 500-1000 товаров

**Полная конфигурация (с обработкой фото):**
- + FASHN AI: ~$50/месяц на 1000 товаров
- + Google Vision: $1-5/месяц

**Для часов рекомендую минимальную** - FASHN не нужен.

---

## Безопасность

### Не коммить в git:

```bash
# В .gitignore уже добавлено:
.env
db.sqlite3
media/
```

### Бэкап .env файла

Сохрани `.env` ОТДЕЛЬНО (не в git!):
```bash
cp .env ~/Dropbox/shoesbot_env_backup.txt
```

---

## FAQ

**Q: Можно без Telegram бота?**
A: Нет, Telegram - основной интерфейс загрузки фото.

**Q: Работает ли на Windows?**
A: Да, но нужны небольшие изменения путей.

**Q: Можно деплоить на сервер?**
A: Да, есть Dockerfile и инструкции для Railway/Render/Heroku.

**Q: Сколько товаров может обработать?**
A: Ограничений нет. Проверено на 1000+ карточках.

**Q: Можно продавать на eBay/Shopify?**
A: Да, есть интеграция (нужна настройка).

---

## Контакты

GitHub: https://github.com/ipochtoy/shoesbot

