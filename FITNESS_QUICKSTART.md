# Быстрый старт: TrainingPeaks + MyNetDiary → Telegram

## Что делает система

1. Собирает данные о тренировках из TrainingPeaks
2. Собирает данные о питании из MyNetDiary  
3. Генерирует отчет через ChatGPT
4. Отправляет в Telegram (вручную по `/report` или автоматически каждый день в 21:00)

## Минимальная настройка (5 минут)

### 1. Добавьте в `.env`:

```bash
# Обязательно
OPENAI_API_KEY=sk-...  # Ваш OpenAI API ключ
FITNESS_REPORT_CHAT_ID=123456789  # Ваш Telegram Chat ID (получить через @userinfobot)

# Опционально - TrainingPeaks
TRAININGPEAKS_CLIENT_ID=...
TRAININGPEAKS_CLIENT_SECRET=...
TRAININGPEAKS_ACCESS_TOKEN=...
TRAININGPEAKS_REFRESH_TOKEN=...

# Опционально - MyNetDiary (CSV экспорт проще всего)
MYNETDIARY_CSV_PATH=/path/to/export.csv
```

### 2. Установите зависимости:

```bash
pip install -r requirements.txt
```

### 3. Запустите бота:

```bash
python bot.py
```

### 4. Проверьте:

Отправьте `/report` в Telegram бот.

## Полная настройка

См. `FITNESS_SETUP.md` для детальной инструкции по настройке TrainingPeaks API и MyNetDiary.

## Команды

- `/report` - получить отчет сейчас
- Кнопка "🔄 Обновить отчет" - обновить отчет

## Автоматическая отправка

Отчеты отправляются автоматически каждый день в **21:00** (можно изменить в `daily_fitness_scheduler.py`).

