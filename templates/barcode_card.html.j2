🛍️ КАРТОЧКА ТОВАРА
==============
Фото: {{ photo_count }} шт.
Найдено кодов: {{ barcodes|length }}
{% if barcodes and barcodes|length > 0 %}
📊 НАЙДЕННЫЕ КОДЫ:
{% for b in barcodes %}{{ loop.index }}. {% if b.data.startswith('GG') or 'GG' in b.data %}Наша лейба GG: {% endif %}{{ b.data }}
{% endfor %}
==============
{% endif %}
