🛍️ КАРТОЧКА ТОВАРА
━━━━━━━━━━━━━━━━
📸 Фото: {{ photo_count }} шт.  |  🏷️ Коды: {{ barcodes|length }}
{% if barcodes and barcodes|length > 0 %}

📊 НАЙДЕННЫЕ КОДЫ:
{% for b in barcodes %}{{ loop.index }}️⃣ {{ b.data }}
{% endfor %}━━━━━━━━━━━━━━━━
{% endif %}

