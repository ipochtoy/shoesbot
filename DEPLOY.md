# Как деплоить изменения на VM

## Процесс

### 1. Локально (на своем компьютере)

Делаешь правки в коде, потом:

```bash
cd ~/Projects/shoesbot

# Коммитишь изменения
git add .
git commit -m "описание изменений"

# Пушишь в GitHub
git push origin main
```

### 2. На VM (в терминале сервера)

Просто запусти скрипт деплоя:

```bash
cd ~/shoesbot
./deploy.sh
```

Всё! Скрипт автоматически:
- Скачает изменения из GitHub
- Соберет статику Django
- Перезапустит бота и Django

---

## Первый раз настройка

Если еще не сделал, один раз выполни на VM:

```bash
cd ~/shoesbot
git pull origin main
chmod +x deploy.sh
```

Теперь можешь использовать `./deploy.sh` каждый раз.

---

## Быстрая шпаргалка

**Локально:**
```bash
git add .
git commit -m "fix"
git push
```

**На VM:**
```bash
./deploy.sh
```

Готово! 🎉

---

## Если что-то пошло не так

### Git pull выдает ошибку "conflicts"

```bash
# На VM
cd ~/shoesbot
git stash  # Спрячет локальные изменения
git pull origin main
git stash pop  # Вернет локальные изменения
```

### Нужно откатить изменения

```bash
# На VM
cd ~/shoesbot
git log  # Посмотри список коммитов
git reset --hard COMMIT_ID  # Откатись к нужному коммиту
./deploy.sh
```

### Проверить статус сервисов

```bash
sudo systemctl status shoesbot.service
sudo systemctl status shoesdjango.service
```

### Посмотреть логи

```bash
tail -f ~/shoesbot/bot.log
tail -f ~/shoesbot/django.log
```

