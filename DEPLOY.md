# Как деплоить изменения на VM

## 🚀 Быстрый деплой из Cursor (одна команда)

### Вариант 1: Через скрипт

В терминале Cursor:

```bash
./deploy_from_local.sh
```

Скрипт автоматически:
- Коммитит изменения (спросит описание)
- Пушит в GitHub
- Деплоит на VM
- Перезапускает сервисы

### Вариант 2: Через меню (еще проще!)

1. Нажми `Cmd+Shift+P`
2. Введи: `Tasks: Run Task`
3. Выбери: `🚀 Deploy to VM`

Готово! ✨

---

## Другие полезные задачи (Tasks)

В Cursor: `Cmd+Shift+P` → `Tasks: Run Task` → выбери:

- **📊 VM Status** — статус бота и Django
- **📋 Bot Logs** — логи бота в реальном времени
- **📋 Django Logs** — логи Django в реальном времени
- **🔄 Restart Services** — перезапустить бот и Django

---

## Ручной процесс (если нужен контроль)

### 1. Локально (на своем компьютере)

Делаешь правки в коде, потом:

```bash
cd ~/Projects/shoesbot

# Коммитишь изменения
git add .
git commit -m "описание изменений"

# Пушишь в GitHub
git push origin main
```

### 2. На VM (в терминале сервера)

Просто запусти скрипт деплоя:

```bash
cd ~/shoesbot
./deploy.sh
```

Всё! Скрипт автоматически:
- Скачает изменения из GitHub
- Соберет статику Django
- Перезапустит бота и Django

---

## Если что-то пошло не так

### Git pull выдает ошибку "conflicts"

```bash
# На VM
cd ~/shoesbot
git stash  # Спрячет локальные изменения
git pull origin main
git stash pop  # Вернет локальные изменения
```

### Нужно откатить изменения

```bash
# На VM
cd ~/shoesbot
git log  # Посмотри список коммитов
git reset --hard COMMIT_ID  # Откатись к нужному коммиту
./deploy.sh
```

### Проверить статус сервисов

```bash
sudo systemctl status shoesbot.service
sudo systemctl status shoesdjango.service
```

### Посмотреть логи

```bash
tail -f ~/shoesbot/bot.log
tail -f ~/shoesbot/django.log
```

---

## Быстрая шпаргалка

**Самый простой способ:**
```bash
./deploy_from_local.sh
```

**Или через Cursor:**
- `Cmd+Shift+P` → `Tasks: Run Task` → `🚀 Deploy to VM`

Готово! 🎉
