#!/bin/bash
# Запуск Django на порту 8001 для разработки/тестирования
# Production бот продолжает работать на порту 8000

cd "$(dirname "$0")/shoessite" || exit 1

# Используем отдельный порт для разработки
PORT=${1:-8001}

echo "🚀 Запуск Django DEV на порту $PORT"
echo "📝 Production Django работает на 8000 (бот использует его)"
echo "🔧 DEV Django будет на $PORT (для тестирования изменений)"
echo ""

python3 manage.py runserver 0.0.0.0:$PORT

